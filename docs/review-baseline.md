# Greptile Full Codebase Baseline Review

This commit intentionally adds a docs-only file to trigger a repository-wide baseline review.

Requested focus areas:
- Security invariants (auth, SSRF, command execution, trust boundaries)
- Sigma rule quality and false-positive risk
- Missing regression tests for security-sensitive paths
- Reliability/performance pitfalls and unsafe defaults
