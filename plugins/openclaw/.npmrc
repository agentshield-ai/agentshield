# openclaw is a peerDependency (the host runtime supplies it). Omit it from
# installs and the committed lockfile so the plugin's own dev/CI tree stays
# lean and SCA scanners don't flag the host runtime's transitive CVEs here.
omit=peer
