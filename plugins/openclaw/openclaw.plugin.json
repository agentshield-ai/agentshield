{
  "id": "agentshield",
  "version": "2.0.0",
  "description": "Real-time security evaluation for AI agent tool calls with advanced triage and feedback capabilities",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": { "type": "boolean", "default": true },
      "endpoint": { "type": "string", "default": "http://127.0.0.1:8433/api/v1/evaluate" },
      "auth_token": { "type": "string" },
      "timeout_ms": { "type": "number", "default": 200, "minimum": 5, "maximum": 5000 },
      "timeout_policy": { "type": "string", "enum": ["allow", "block", "log"], "default": "block" },
      "notify": { "type": "string", "enum": ["all", "high", "critical", "none"], "default": "high", "description": "Minimum severity level for user notifications" },
      "intercept": {
        "type": "array",
        "items": { "type": "string" },
        "default": ["exec", "write", "edit", "read", "browser", "message", "sessions_spawn"]
      },
      "skip": {
        "type": "array",
        "items": { "type": "string" },
        "default": ["session_status"]
      },
      "circuit_breaker": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "failure_threshold": { "type": "number", "default": 3 },
          "recovery_interval_ms": { "type": "number", "default": 30000 }
        }
      }
    }
  }
}
